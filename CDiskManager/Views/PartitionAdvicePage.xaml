<Page
    x:Class="CDiskManager.Views.PartitionAdvicePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:CDiskManager.Models"
    Loaded="Page_Loaded">

    <Grid Padding="36,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Spacing="8">
                <TextBlock Text="分区建议" Style="{StaticResource TitleTextBlockStyle}"/>
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                                  Width="16" Height="16"/>
                    <TextBlock Text="{x:Bind ViewModel.StatusText, Mode=OneWay}"
                               VerticalAlignment="Center"
                               Style="{StaticResource BodyTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </StackPanel>
            <Button Grid.Column="1" VerticalAlignment="Top"
                    Command="{x:Bind ViewModel.LoadCommand}"
                    IsEnabled="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolNegation}}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                    <TextBlock Text="重新分析"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Partition overview -->
        <ItemsRepeater Grid.Row="1" ItemsSource="{x:Bind ViewModel.Partitions, Mode=OneWay}" Margin="0,16,0,16">
            <ItemsRepeater.Layout>
                <StackLayout Spacing="8"/>
            </ItemsRepeater.Layout>
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="models:PartitionInfo">
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="8" Padding="16">
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{x:Bind DriveLetter}"
                                       FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" Spacing="4">
                                <ProgressBar Value="{x:Bind UsagePercent}" Maximum="100" Height="8"
                                             Foreground="{x:Bind UsagePercent, Converter={StaticResource UsageColor}}"/>
                                <TextBlock Style="{StaticResource CaptionTextBlockStyle}">
                                    <Run Text="{x:Bind UsedFormatted}"/>
                                    <Run Text=" 已用 / "/>
                                    <Run Text="{x:Bind TotalFormatted}"/>
                                    <Run Text=" 总计"/>
                                </TextBlock>
                            </StackPanel>
                            <TextBlock Grid.Column="2" VerticalAlignment="Center" FontWeight="SemiBold">
                                <Run Text="{x:Bind FreeFormatted}"/>
                                <Run Text=" 可用"/>
                            </TextBlock>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>

        <!-- Suggestions -->
        <Grid Grid.Row="2">
            <ListView ItemsSource="{x:Bind ViewModel.Suggestions, Mode=OneWay}"
                      SelectionMode="None"
                      Visibility="{x:Bind ViewModel.HasSuggestions, Mode=OneWay}">
                <ListView.Header>
                    <TextBlock Text="迁移建议" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,12"/>
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:MigrationSuggestion">
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,4">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <FontIcon Grid.Column="0" Glyph="&#xE8DE;" FontSize="22"
                                          VerticalAlignment="Center"
                                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                                <StackPanel Grid.Column="1" Spacing="4">
                                    <TextBlock FontWeight="SemiBold">
                                        <Run Text="{x:Bind FolderName}"/>
                                        <Run Text=" — "/>
                                        <Run Text="{x:Bind SizeFormatted}"/>
                                    </TextBlock>
                                    <TextBlock Text="{x:Bind Reason}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               TextWrapping="Wrap"/>
                                    <TextBlock Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                        <Run Text="{x:Bind CurrentPath}"/>
                                        <Run Text="  →  "/>
                                        <Run Text="{x:Bind SuggestedPath}"/>
                                    </TextBlock>
                                </StackPanel>
                                <Button Grid.Column="2" Content="打开文件夹" VerticalAlignment="Center"
                                        Click="OpenFolder_Click" Tag="{x:Bind CurrentPath}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Empty state -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12"
                        Visibility="{x:Bind ViewModel.ShowEmptyState, Mode=OneWay}">
                <FontIcon Glyph="&#xE73E;" FontSize="48"
                          Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                <TextBlock Text="C 盘状态良好，暂无迁移建议"
                           HorizontalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
